<%= form_with model: task do |f| %>
  <%= f.date_field :date,
              as: :string,
              class: "custom-input back-gray-input", value: Date.today,
              input_html: { data: { controller: "datepicker" } } %>
  <%= button_tag :submit,  class: "h-64 btn btn-warning text-white w-100 rounded-50 p-3 fs-5" do %>
    <%= "C'est calé !" %>
  <% end %>
<% end %>
