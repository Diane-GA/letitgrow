<div class="mobile-container bg-primary text-white d-flex flex-column align-items-center padding-horizontal-15 padding-bottom-120">
  <%= render "shared/title" %>
  <div class="icon">
    <%= image_tag "let-it-grow-logo.svg" %>
  </div>
  <div class = "catchphrase mb-4 text-center ">
    De la graine à la récolte,<br>laisse-toi guider !
  </div>
  <% if user_signed_in? %>
    <!-- TODO: Faire le contenu de la partial -->
    <%= render "user_welcome"%>
  <%else%>
    <%= render "shared/login" %>
  <%end%>
</div>
