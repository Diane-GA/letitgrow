<div class="mobile-container bg-primary text-white d-flex flex-column align-items-center padding-horizontal-15">
  <%= render "shared/title" %>
  <div class="logo">
    <%= image_tag "let-it-grow-logo.svg" %>
  </div>
  <div class = "catchphrase text-center ">
    De la graine à la récolte,<br>laisse-toi guider !
  </div>
  <% if user_signed_in? %>
    <%= render "user_welcome"%>
  <%else%>
    <%= render "shared/login" %>
  <%end%>
</div>
